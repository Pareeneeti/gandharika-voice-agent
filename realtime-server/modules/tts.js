export async function synthesizeSpeech(text) {
  // 🔊 this will later call OpenAI TTS or ElevenLabs
  console.log("🎶 Generating voice for:", text);
  // fake return
  return "base64-audio-data-goes-here";
}
